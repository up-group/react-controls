import { Meta, Story, Preview, Prop } from '@storybook/addon-docs/blocks';
import UpSelect from './';

<Meta title="MDX/UpSelect" component={UpSelect} />

# UpSelect

With `UpSelect` we can define a component display..

<Preview>
  <Story name="all checkboxes">
    <UpSelect />
  </Story>
</Preview>

<Prop of={UpSelect} />