import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import UpSelect from './UpSelect';
import UpParagraph from '../../Display/Paragraph/UpParagraph'

import {General, CustomTableComponent} from './index.stories.tsx'

<Meta title="Docs/Inputs/UpSelect" component={UpSelect} />

# UpSelect

With `UpSelect` we can define a component display..
<Preview title="UpSelect">
    <UpSelect data={[
                { id: 1, text: 'M.' },
                { id: 2, text: 'Mme' },
                { id: 3, text: 'Mlle' },
                { id: 4, text: 'Dr' },
            ]} />
</Preview>

# Properties

<Props of={UpSelect}  />

# Simple usage sample 

Here a simple sample of using the UpSelect component.

<Preview title="Simple usage of UpSelect">
    <General />
</Preview>

# Properties

<CustomTableComponent />