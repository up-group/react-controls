import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import UpSelect from './UpSelect';

<Meta title="Components|Inputs/UpSelect" component={UpSelect} />

# UpSelect

With `UpSelect` we can define a component display..

<Preview>
    <UpSelect />
</Preview>

# Properties 

<Props />
